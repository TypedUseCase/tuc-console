#!/usr/bin/env bash

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

NAME="TucConsole"
CONSOLE="$SCRIPT_DIR/Debug/net5.0/$NAME.dll"

if [ ! -f "$CONSOLE" ]; then
    CONSOLE="$SCRIPT_DIR/Release/net5.0/$NAME.dll"
fi

dotnet "$CONSOLE" "$@"
